                                                        Переводим проект `py_base_minimum` на HTTPS

Проблема: сейчас проекты используют два разных способа подключения:

- `qa-auto-course` — использует HTTPS (работает)
- `py_base_minimum` — использует SSH (проблемный, из-за `.ssh/config`)

Необходимо перевести `py_base_minimum` на HTTPS. Вот как это сделать правильно.


                                                                 Шаги            

1. Перейди в папку проекта:
cd D:\py_base_minimum

2. Посмотри текущие remote
git remote -v


3. Замени SSH-адрес на HTTPS
git remote set-url origin https://gitlab.com/aivenkis92/py_base_minimum.git


4. Проверь, что изменилось
git remote -v

Должно стать примерно так:

origin  https://gitlab.com/aivenkis92/py_base_minimum.git (fetch)
origin  https://gitlab.com/aivenkis92/py_base_minimum.git (push)


После изменения попробуй запушить:
git push -u origin master


________________________________________________________________________________________
Чтобы PyCharm отправлял изменения в GitHub и GitLab одновременно одной кнопкой, нужно объединить оба адреса в рамках одной удаленной ветки (remote), 
либо настроить автоматическое дублирование.
Самый удобный и надежный способ — настроить один Git remote с двумя URL-адресами. В этом случае при нажатии Commit and Push PyCharm отправит код на оба сервера.

                                                Пошаговая инструкция

Шаг 1. Добавьте второй URL через терминал
PyCharm не позволяет добавить два URL-адреса для одного пуша через графический интерфейс, поэтому используйте встроенный терминал.

    1. Убедитесь, что один из репозиториев (например, GitHub) уже подключен как origin. Проверить это можно командой:
git remote -v

    2. Добавьте URL-адрес второго репозитория (GitLab) в этот же origin для отправки кода:
git remote set-url --add --push origin URL_ВАШЕГО_РЕПОЗИТОРИЯ_GITLAB

    3. Чтобы при пуше обновлялся и первый репозиторий (GitHub), обязательно продублируйте его адрес этой же командой 
    (если этого не сделать, push будет идти только в GitLab):
git remote set-url --add --push origin URL_ВАШЕГО_РЕПОЗИТОРИЯ_GITHUB

Шаг 2. Проверьте результат
Введите в терминале команду проверки: git remote -v

Сбросьте настройки push, чтобы удалить ошибочную строку. Выполните команду:
git remote set-url --delete --push origin URL_ВАШЕГО_РЕПОЗИТОРИЯ_GITLAB
_________________________________________________________________________________